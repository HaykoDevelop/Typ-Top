// A =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyA');
    if (event.key === 'a' || event.key === 'A') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyA');
    if (event.key === 'a' || event.key === 'A') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// Z =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyZ');
    if (event.key === 'z' || event.key === 'Z') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyZ');
    if (event.key === 'z' || event.key === 'Z') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// E =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyE');
    if (event.key === 'e' || event.key === 'E') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyE');
    if (event.key === 'e' || event.key === 'E') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// R =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyR');
    if (event.key === 'r' || event.key === 'R') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyR');
    if (event.key === 'r' || event.key === 'R') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// T =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyT');
    if (event.key === 't' || event.key === 'T') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyT');
    if (event.key === 't' || event.key === 'T') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// Y =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyY');
    if (event.key === 'y' || event.key === 'Y') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyY');
    if (event.key === 'y' || event.key === 'Y') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// U =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyU');
    if (event.key === 'u' || event.key === 'U') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyU');
    if (event.key === 'u' || event.key === 'U') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// I =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyI');
    if (event.key === 'i' || event.key === 'I') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyI');
    if (event.key === 'i' || event.key === 'I') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// O =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyO');
    if (event.key === 'o' || event.key === 'O') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyO');
    if (event.key === 'o' || event.key === 'O') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// P =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyP');
    if (event.key === 'p' || event.key === 'P') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyP');
    if (event.key === 'p' || event.key === 'P') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// Q =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyQ');
    if (event.key === 'q' || event.key === 'Q') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyQ');
    if (event.key === 'q' || event.key === 'Q') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// S =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyS');
    if (event.key === 's' || event.key === 'S') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyS');
    if (event.key === 's' || event.key === 'S') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// D =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyD');
    if (event.key === 'd' || event.key === 'D') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyD');
    if (event.key === 'd' || event.key === 'D') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// F =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyF');
    if (event.key === 'f' || event.key === 'F') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyF');
    if (event.key === 'f' || event.key === 'F') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// G =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyG');
    if (event.key === 'g' || event.key === 'G') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyG');
    if (event.key === 'g' || event.key === 'G') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// H =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyH');
    if (event.key === 'h' || event.key === 'H') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyH');
    if (event.key === 'h' || event.key === 'H') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// J =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyJ');
    if (event.key === 'j' || event.key === 'J') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyJ');
    if (event.key === 'j' || event.key === 'J') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// K =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyK');
    if (event.key === 'k' || event.key === 'K') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyK');
    if (event.key === 'k' || event.key === 'K') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// L =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyL');
    if (event.key === 'l' || event.key === 'L') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyL');
    if (event.key === 'l' || event.key === 'L') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// M =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyM');
    if (event.key === 'm' || event.key === 'M') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyM');
    if (event.key === 'm' || event.key === 'M') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// W =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyW');
    if (event.key === 'w' || event.key === 'W') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyW');
    if (event.key === 'w' || event.key === 'W') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// X =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyX');
    if (event.key === 'x' || event.key === 'X') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyX');
    if (event.key === 'x' || event.key === 'X') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// C =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyC');
    if (event.key === 'c' || event.key === 'C') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyC');
    if (event.key === 'c' || event.key === 'C') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// V =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyV');
    if (event.key === 'v' || event.key === 'V') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyV');
    if (event.key === 'v' || event.key === 'V') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// B =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyB');
    if (event.key === 'b' || event.key === 'B') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyB');
    if (event.key === 'b' || event.key === 'B') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});

// N =================================================================================
document.addEventListener('keydown', (event) => {
    const key = document.querySelector('.keyN');
    if (event.key === 'n' || event.key === 'N') {
        key.style.backgroundColor = 'var(--color2)';
        key.style.color = 'var(--color1)';
    }
});
document.addEventListener('keyup', (event) => {
    const key = document.querySelector('.keyN');
    if (event.key === 'n' || event.key === 'N') {
        key.style.backgroundColor = 'var(--color1)';
        key.style.color = 'var(--color2)';
    }
});